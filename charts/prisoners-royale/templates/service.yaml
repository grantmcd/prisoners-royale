apiVersion: v1
kind: Service
metadata:
  name: {{ include "prisoners-royale.fullname" . }}
  labels:
    {{- include "prisoners-royale.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prisoners-royale.selectorLabels" . | nindent 4 }}
